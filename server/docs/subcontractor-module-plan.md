# แผนบัญชี — หัวข้อ 2 ผู้รับเหมาช่วง (Subcontractor)

สถานะ: **ร่างแผนเท่านั้น ยังไม่มีโค้ด/migration ใดๆ** — ต้องผ่านการยืนยันจากฝ่ายบัญชีก่อน (จุดที่ไม่แน่ใจ
ทำเครื่องหมาย ⚠️ ไว้ตามรูปแบบเดียวกับ `accounting-review-checklist.md`) — บันทึกไว้กันหายซ้ำ (5 ส่วนนี้
เคยคุยกันในแชทมาก่อนแล้ว ไม่เคยบันทึกลงไฟล์ จนหายไปตอน DB restore เมื่อ 2026-08-17)

ร่างเมื่อ: 2026-08-18

---

## ผังบัญชีที่ตกลงแล้ว (ยืนยันจากผู้ใช้แล้ว)

| รหัส | ชื่อบัญชี | ประเภท | หมายเหตุ |
|---|---|---|---|
| `2130` | เจ้าหนี้ผู้รับเหมาช่วง | หนี้สิน | ยอดสุทธิที่ต้องจ่ายจริงหลังหักทุกรายการ |
| `2140` | เงินประกันผลงานค้างจ่าย | หนี้สิน | สมมาตรกับ `1250 ลูกหนี้เงินประกันผลงาน` ที่มีอยู่แล้ว (ฝั่งลูกค้าเรา หัก retention เรา ↔ ฝั่งเรา หัก retention ผู้รับเหมา) |

**⚠️ ต้องเพิ่มอีก 1 บัญชีที่ยังไม่เคยเสนอ**: `1160 เงินจ่ายล่วงหน้าผู้รับเหมาช่วง` (สินทรัพย์) — ดูเหตุผล
เต็มในส่วน 1 ด้านล่าง — **ไม่ควรใช้ `1150 ลูกหนี้เงินทดรองจ่าย` ร่วมกัน** เพราะ 1150 ผูกกับพนักงาน
(เจ้าหนี้/ลูกหนี้คนละฝ่ายกัน ผสมกันจะทำให้ยอดคงเหลือ 1150 อ่านไม่ออกว่าเป็นของพนักงานหรือผู้รับเหมา)

WHT default: **`40_7` ค่าจ้างทำของ/รับเหมาก่อสร้าง (มาตรา 40(7)) 3%** ดึงจาก `client_wht_income_types`
เสมอ ไม่ hardcode — override ได้ระดับสัญญา (เก็บที่ `client_subcontract_terms.wht_rate`,
`NULL` = ใช้ default จาก master ตอนคำนวณจริง, ไม่ใช่ copy ค่าตายตัวมาตอนสร้างสัญญา)

---

## ส่วน 1 — เคส 2.1 เงินล่วงหน้า (Advance Payment)

### บัญชีใหม่ที่ต้องมี
**ต้องมีบัญชีใหม่ — ใช้บัญชีเดิมไม่ได้** เสนอ `1160 เงินจ่ายล่วงหน้าผู้รับเหมาช่วง` (สินทรัพย์ ประเภท
เดียวกับ `1150`/`1200` — เป็นเงินที่จ่ายออกไปแล้วแต่ยังไม่ตัดเป็นต้นทุนจริง จนกว่าจะมีงวดงานมาหักคืน)

### ผัง Dr/Cr ตอนจ่ายเงินล่วงหน้า
ตัวอย่างจริงตามที่ให้มา: สัญญา 1,000,000, จ่ายล่วงหน้า 15% = 150,000

```
Dr 1160 เงินจ่ายล่วงหน้าผู้รับเหมาช่วง        150,000
   Cr 2120 ภาษีหัก ณ ที่จ่ายค้างนำส่ง                  4,500   (150,000 × 3% ⚠️ ดูหัวข้อ "มี WHT ไหม" ด้านล่าง)
   Cr 1100 เงินสด/ธนาคาร                             145,500
```

### มี VAT ไหม — ⚠️ ไม่แน่ใจ ต้องยืนยัน
ภาษีมูลค่าเพิ่มสำหรับบริการ (รวมงานรับเหมาก่อสร้าง) เกิดขึ้น ณ วันที่ **เร็วที่สุด** ระหว่างวันที่ชำระเงิน
กับวันที่ออกใบกำกับภาษี — เงินล่วงหน้าคือการชำระเงินจริง จึงมีโอกาสเข้าเงื่อนไขต้องมี VAT ทันที **ถ้า**
ผู้รับเหมาออกใบกำกับภาษีให้ตอนรับเงินล่วงหน้า แต่ถ้าผู้รับเหมาเก็บใบกำกับภาษีไว้ออกตอนงวดงานจริงแทน
(บางรายทำแบบนี้) ก็ไม่มี VAT ตอนนี้ — **ขึ้นอยู่กับพฤติกรรมผู้รับเหมาแต่ละรายจริง ไม่ใช่กฎตายตัว** ระบบ
ควรออกแบบให้ยืดหยุ่น (มี toggle `has_tax_invoice` ต่อรายการเหมือนโมดูล 1.3/1.4) ไม่ใช่ hardcode ว่ามี/ไม่มี

### มี WHT ไหม — ⚠️ ไม่แน่ใจ ต้องยืนยัน (เสนอ default = มี)
เงินล่วงหน้าคือ "การจ่ายเงินได้" จริงตามความหมายมาตรา 3 เตรส — หลักการทั่วไปคือ **ต้องหัก ณ ที่จ่ายทันที
ที่มีการจ่ายเงิน ไม่ว่าจะเป็นเงินล่วงหน้าหรือเงินงวดปกติ** เสนอให้ default เป็น **มี WHT** ตามตัวอย่างที่คำนวณ
ไว้ด้านบน (4,500 บาท) — แต่ยังต้องยืนยันกับฝ่ายบัญชีว่าตรงกับแนวปฏิบัติจริงของบริษัท เพราะบางบริษัทเลือก
หัก WHT ทีเดียวตอนเคลียร์งวดสุดท้ายแทน (ไม่ใช่แนวทางที่ถูกต้องตามกฎหมายเป๊ะ แต่พบเป็นแนวปฏิบัติจริงได้)

---

## ส่วน 2 — การหักคืนเงินล่วงหน้าใน 2.2 (จุดซับซ้อนที่สุด)

ตัวอย่างจริงตามที่ให้มา:
```
สัญญา                 1,000,000
จ่ายล่วงหน้า 15%          150,000  (จ่ายไปแล้วในส่วน 1)
งวดนี้เบิก               200,000
หักคืนเงินล่วงหน้า 15%      30,000  (= 200,000 × 15%)
กัน retention 5%           10,000  (= 200,000 × 5%)
WHT 3%
```

### ⚠️ WHT คำนวณจากฐานไหน — จุดที่ไม่แน่ใจที่สุดในแผนทั้งหมด

มี 2 ทางที่เป็นไปได้ ทั้งคู่มีเหตุผลรองรับ ต้องให้ฝ่ายบัญชีชี้ขาด:

**ทางที่ 1 (เสนอเป็น default)**: ฐานคือ **มูลค่างวดงานเต็มจำนวนที่รับรอง (200,000) — ไม่ลดฐานด้วย
retention หรือเงินล่วงหน้าที่หักคืน** → WHT = 200,000 × 3% = **6,000**
เหตุผลสนับสนุน: มูลค่างานที่ "เกิดขึ้นจริง/ถูกรับรองแล้ว" ในงวดนี้คือ 200,000 เต็มจำนวน — retention
เป็นแค่การเลื่อนจ่ายเงินสด (งานยังเป็นของผู้รับเหมา ยังไม่ได้ลดมูลค่างานลง) และเงินล่วงหน้าที่หักคืนก็เป็น
แค่การหักกลบหนี้เดิมที่เคยจ่ายไปแล้ว (ไม่ใช่ลดมูลค่างานงวดนี้) แนวทางนี้ตรงกับที่ใบกำกับภาษี VAT มักออก
บนมูลค่างวดเต็มจำนวนเสมอ (retention/เงินล่วงหน้าไม่ใช่ตัวลดฐาน VAT) — ใช้ตรรกะเดียวกันกับ WHT

**ทางที่ 2**: ฐานคือ **ยอดที่จ่ายเงินสดจริงเท่านั้น** (200,000 - 30,000 หักคืนล่วงหน้า - 10,000
retention = 160,000) → WHT = 160,000 × 3% = 4,800
เหตุผลสนับสนุน: หน้าที่หัก ณ ที่จ่ายผูกกับ "การจ่ายเงิน" จริง — ส่วนที่ยังไม่จ่าย (retention) หรือหักกลบไป
แล้ว (เงินล่วงหน้าที่เคยหัก WHT ไปแล้วตอนจ่ายจริง) ไม่ควรถูกนับซ้ำเป็นฐานคำนวณอีกรอบ

**ผลต่างระหว่าง 2 ทาง = 1,200 บาทต่องวด** (6,000 vs 4,800) — ไม่ใช่เรื่องเล็ก ต้องเลือกให้ถูกก่อนเขียนโค้ด
จริง เอกสารนี้จะ**ยึดทางที่ 1 เป็น default ในตัวอย่างคำนวณด้านล่างทั้งหมด** จนกว่าฝ่ายบัญชีจะแก้ไข

### retention คำนวณจากฐานไหน
**ไม่กำกวมเท่า WHT** — ตามธรรมเนียมทั่วไปและตรงกับที่ผู้ใช้ให้ตัวเลขมา: retention คำนวณจาก **มูลค่างวด
งานเต็มจำนวน (200,000) เสมอ** ไม่ใช่ยอดหลังหักอะไรทั้งสิ้น = 200,000 × 5% = 10,000 ตรงกับตัวอย่างที่ให้มา
พอดี — เหตุผล: retention คือ "ประกันผลงาน" ของมูลค่างานที่ส่งมอบ ไม่เกี่ยวกับกลไกการจ่ายเงินเลย

### ลำดับการหัก (มีผลต่อยอดสุทธิ)
ยืนยันว่า**ลำดับไม่กระทบยอดสุทธิสุดท้าย** เพราะทุกรายการ (หักคืนเงินล่วงหน้า, retention, WHT) คำนวณจาก
ฐาน "มูลค่างวดเต็มจำนวน 200,000" อิสระต่อกันทั้งหมด ไม่มีรายการไหนคำนวณจากผลลัพธ์ของอีกรายการ (ต่างจาก
เช่น ถ้า WHT ต้องคำนวณจากยอดหลังหัก retention ก่อน แบบนั้นลำดับจะมีผลจริง) — **ข้อนี้เป็นเหตุผลเพิ่มเติมที่
สนับสนุนทางที่ 1 ของคำถาม WHT ด้านบน**: ถ้าเลือกทางที่ 1 (ฐาน WHT = มูลค่างวดเต็ม) การคำนวณทั้งหมดจะ
ทำแบบขนานกันได้ ไม่ต้องกังวลเรื่องลำดับเลย ซึ่งเขียนโค้ดและตรวจสอบง่ายกว่าทางที่ 2 มาก (ทางที่ 2 ต้อง
คำนวณ retention/advance recovery ก่อน แล้วค่อยคำนวณ WHT จากผลลัพธ์ ทำให้ลำดับมีผลจริง)

### ผู้รับเหมาได้เงินจริงเท่าไหร่ (ใช้ทางที่ 1 เป็นฐานคำนวณ)
```
มูลค่างวดงาน                    200,000
หัก เงินล่วงหน้าที่หักคืน          (30,000)
หัก เงินประกันผลงาน (retention)   (10,000)
หัก ภาษีหัก ณ ที่จ่าย (WHT 3%)      (6,000)
                              ─────────
สุทธิที่จ่ายจริง                  154,000
```

### ผัง Dr/Cr ตอน approve งวดงาน
```
Dr 5200 ต้นทุนผู้รับเหมาช่วง                200,000   (มูลค่างวดเต็มจำนวน)
   Cr 1160 เงินจ่ายล่วงหน้าผู้รับเหมาช่วง             30,000   (หักคืนเงินล่วงหน้า)
   Cr 2140 เงินประกันผลงานค้างจ่าย                10,000   (กัน retention)
   Cr 2120 ภาษีหัก ณ ที่จ่ายค้างนำส่ง                 6,000   (WHT — ทางที่ 1 ⚠️)
   Cr 2130 เจ้าหนี้ผู้รับเหมาช่วง                  154,000   (สุทธิที่ต้องจ่ายจริง)
```
ตรวจสมดุล: Dr 200,000 = Cr (30,000+10,000+6,000+154,000) = 200,000 ✓

**ถ้ามี VAT** (ผู้รับเหมาออกใบกำกับภาษีเต็มรูปงวดนี้ — ตัวอย่างผู้ใช้ไม่ได้ระบุ VAT มาด้วย จึงไม่รวมใน
ตัวอย่างหลัก): เพิ่ม `Dr 1170 ภาษีซื้อ = 200,000 × 7% = 14,000` และ `Cr 2130` เพิ่มขึ้นเท่ากับ VAT (ผู้
รับเหมาได้ VAT คืนเต็มจำนวน ไม่ถูกหักด้วย retention/advance recovery — ใช้ gate `has_tax_invoice`
เดียวกับโมดูล 1.3/1.4)

### ผัง Dr/Cr ตอนจ่ายเงินจริง
```
Dr 2130 เจ้าหนี้ผู้รับเหมาช่วง    154,000
   Cr 1100 เงินสด/ธนาคาร               154,000
```

---

## ส่วน 3 — ยอดคงเหลือที่ต้องติดตาม

**ยังไม่มีที่เก็บ** — `client_subcontract_terms` ไม่เคยถูกสร้างจริง (เคยร่างไว้ในแชทช่วง batch 2 เท่านั้น
ไม่เคยบันทึกลงไฟล์ ไม่มีร่องรอยในโค้ด/migration ใดๆ เลย ตรวจสอบแล้วจากทั้ง repo) — เอกสารนี้ร่างคอลัมน์
ใหม่ทั้งหมดในส่วน 4 ด้านล่าง

**หลักการที่ต้องยึดตาม CLAUDE.md**: ยอดคงเหลือทั้ง 3 ตัวนี้ **ต้องเป็นค่าที่คำนวณสด (SUM) ตอนอ่าน ไม่ใช่
คอลัมน์ตัวเลขที่ update สะสมทับ** (แพทเทิร์นเดียวกับ `CLIENT_PETTY_CASH_FUND_BALANCE_SUBQUERY` ที่ใช้อยู่
แล้วในโมดูล 1.1) — ป้องกัน lost-update ตอน concurrent approve เหมือนที่เคยแก้บั๊กจริงมาแล้วในกองทุน
เงินสดย่อย

1. **เงินล่วงหน้าคงค้าง** = `SUM(gross_amount ที่ billing_type='advance' และ approved)` −
   `SUM(advance_recovery_amount ที่ billing_type='progress' และ approved)` ต่อสัญญา
2. **Retention สะสมรายสัญญา** = `SUM(retention_amount ที่ billing_type='progress' และ approved)` −
   `SUM(gross_amount ที่ billing_type='retention_release' และ approved)` ต่อสัญญา
3. **ยอดเบิกสะสมเทียบมูลค่าสัญญา** (กันเบิกเกิน) = `SUM(gross_amount ที่ billing_type='progress' และ
   approved)` ต้องไม่เกิน `contract_value` — เช็คตอน submit/approve คล้ายที่ PR เช็ค budget ceiling
   (เทียบฝั่ง SQL ด้วย `::numeric` เสมอ ไม่ใช้ JS Number ตัดสินใจ ตามกฎ CLAUDE.md ข้อ 3)

---

## ส่วน 4 — List คอลัมน์ (ยังไม่เขียน DDL)

### `client_subcontractors` (master ข้อมูลผู้รับเหมา — คล้าย `client_external_payees` ที่มีอยู่แล้ว)

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | SERIAL PK | |
| `company_id` | INTEGER | FK → `customer_companies(id)` |
| `name` | TEXT NOT NULL | |
| `tax_id` | TEXT | 13 หลัก เหมือน `client_external_payees` |
| `branch_code` | TEXT DEFAULT '00000' | |
| `address` | TEXT DEFAULT '' | |
| `taxpayer_type` | TEXT CHECK (individual/juristic) | |
| `phone` | TEXT DEFAULT '' | |
| `is_active` | BOOLEAN DEFAULT true | |
| `created_at` | TIMESTAMPTZ | |

⚠️ **ไม่เก็บ default WHT rate ไว้ที่ตารางนี้โดยเจตนา** — ตามที่ตกลงไว้ว่า override ได้ "รายสัญญา" ไม่ใช่
"รายผู้รับเหมา" ค่า WHT income type/rate จึงอยู่ที่ `client_subcontract_terms` เท่านั้น (ผู้รับเหมารายเดียว
อาจมีหลายสัญญาที่ WHT ต่างกันได้ในทางทฤษฎี เช่น สัญญาหนึ่งเป็นค่าจ้างทำของ อีกสัญญาเป็นค่าเช่าเครื่องจักร)

### `client_subcontract_terms` (สัญญาต่อโครงการ — ตัวกำหนด % ทั้งหมด)

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | SERIAL PK | |
| `company_id` | INTEGER | FK → `customer_companies(id)` |
| `subcontractor_id` | INTEGER | composite FK → `client_subcontractors(company_id, id)` |
| `project_id` | INTEGER | composite FK → `client_projects(company_id, id)` |
| `contract_no` | TEXT | ออกเลขตอน submit เหมือนเอกสารอื่นทั้งหมดในระบบ (ไม่ใช่ตอนสร้าง draft) |
| `contract_value` | NUMERIC(18,2) NOT NULL CHECK > 0 | |
| `advance_percent` | NUMERIC(5,2) NOT NULL DEFAULT 0 | เช่น 15.00 |
| `retention_percent` | NUMERIC(5,2) NOT NULL DEFAULT 5 | มี pattern เดิมอยู่แล้วที่ `client_projects.default_retention_percent` — ใช้เป็นค่าเริ่มต้นได้ |
| `wht_income_type_code` | TEXT DEFAULT '40_7' | FK → `client_wht_income_types(code)` |
| `wht_rate` | NUMERIC(5,2) | **NULL = ใช้ default จาก master ตอนคำนวณจริงเสมอ** (ไม่ copy ค่ามา snapshot ตอนสร้างสัญญา) มีค่า = override เฉพาะสัญญานี้ |
| `status` | TEXT CHECK (draft/active/completed/terminated) | |
| `start_date`, `end_date` | DATE | |
| `note` | TEXT DEFAULT '' | |
| `created_by`, `created_at` | | |

### `client_subcontract_billings` (ใหม่ — ยังไม่เคยเสนอชื่อนี้มาก่อน แต่จำเป็น)

⚠️ **นี่คือตารางที่ 3 ที่เอกสารเดิมไม่เคยพูดถึง** — ระหว่างร่างพบว่าเงินล่วงหน้า (2.1), งวดงาน (2.2), และ
การคืน retention ตอนจบโครงการ ล้วนเป็น "เอกสารเบิก/จ่ายเงิน" แบบเดียวกัน มีวงจรสถานะ (draft→submitted→
approved) เหมือนกัน จึงเสนอรวมเป็นตารางเดียว คล้าย `client_payment_vouchers` ที่รวม petty_cash/
advance/other ด้วย `voucher_type` แทนที่จะแยก 3 ตาราง — ถ้าไม่เห็นด้วยกับการรวม บอกได้ จะแยกตารางแทน

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | SERIAL PK | |
| `company_id` | INTEGER | FK |
| `subcontract_term_id` | INTEGER | composite FK → `client_subcontract_terms(company_id, id)` |
| `billing_no` | TEXT | ออกตอน submit |
| `billing_type` | TEXT CHECK (advance/progress/retention_release) | |
| `billing_date` | DATE | |
| `gross_amount` | NUMERIC(18,2) NOT NULL | มูลค่างวด/เงินล่วงหน้า/retention ที่คืน แล้วแต่ billing_type |
| `advance_recovery_amount` | NUMERIC(18,2) DEFAULT 0 | เฉพาะ progress — คำนวณฝั่ง server จาก `advance_percent × gross_amount` แล้ว cap ไม่ให้เกินยอดเงินล่วงหน้าคงค้างจริง (ส่วน 3 ข้อ 1) |
| `retention_amount` | NUMERIC(18,2) DEFAULT 0 | เฉพาะ progress |
| `has_tax_invoice` | BOOLEAN DEFAULT false | เกท VAT เหมือนโมดูล 1.3/1.4 |
| `vat_rate`, `vat_amount` | NUMERIC | |
| `wht_income_type_code`, `wht_rate`, `wht_amount` | | snapshot ตอน approve (freeze เหมือนที่แก้บั๊ก `client_wht_income_types` ไปแล้ว) |
| `net_payable_amount` | NUMERIC(18,2) | คำนวณฝั่ง server เสมอ ไม่เชื่อ client |
| `status` | TEXT CHECK (draft/submitted/approved/rejected/cancelled/voided) | |
| `submitted_by/at`, `approved_by/at`, `rejected_reason` | | composite FK ทุกจุดตามกฎ CLAUDE.md ข้อ 1 |
| `voided_reason/by/at` | | เผื่ออนาคตต้อง `/void` เหมือนที่ ก.3 ของโมดูล 1.3 ยังค้างอยู่ |
| `created_by`, `created_at` | | |

---

## ส่วน 5 — Endpoint list + จุดออก 50 ทวิ + idempotency

| Endpoint | Idempotency? | หมายเหตุ |
|---|---|---|
| `GET/POST/PUT /api/customer/subcontractors`, `/:id` | ไม่ต้อง (ไม่เคลื่อนเงิน) | master data |
| `GET/POST/PUT /api/customer/subcontract-terms`, `/:id` | ไม่ต้อง | สัญญา — เปลี่ยนแค่เงื่อนไข ยังไม่เคลื่อนเงิน |
| `GET/POST/PUT /api/customer/subcontract-billings`, `/:id` | **POST ต้อง** (สร้างเอกสารใหม่ กัน double-click ซ้ำ) | |
| `POST .../subcontract-billings/:id/submit` | **ต้อง** | ออกเลขที่เอกสารตรงนี้ |
| `POST .../subcontract-billings/:id/approve` | **ต้อง** | จุดที่โพสต์ journal entry จริง + **ออก 50 ทวิ ที่นี่** (ถ้า `wht_amount>0`, `source_type='subcontractor_payment'` — ค่านี้เตรียมไว้แล้วใน CHECK ของ `client_wht_certificates` ตั้งแต่ migration 0001 ไม่ต้องแก้ constraint เพิ่ม) |
| `POST .../subcontract-billings/:id/reject` | ไม่ต้อง (ตามแพทเทิร์นเดิมของ PR — ไม่เคลื่อนเงิน) | ยังต้องผ่าน `canApprove` เช็คสิทธิ์เหมือนเดิม |
| `POST .../subcontract-billings/:id/cancel` | **ต้อง** (ตามแพทเทิร์น PR items ที่ cancel เคลื่อน state ที่มีผลทางบัญชี) | |
| `GET .../subcontract-terms/:id/balance` | ไม่ต้อง (read-only) | คืนยอดคงเหลือทั้ง 3 ตัวจากส่วน 3 |
| `GET .../subcontract-billings/:id/wht-certificates` | ไม่ต้อง | เหมือนโมดูล 1.3/1.4 |

**⚠️ ผลกระทบต่อระบบสิทธิ์อนุมัติที่มีอยู่แล้ว**: ต้องเพิ่ม doc_type ใหม่ `'subcontractor'` เข้า
`client_pr_approval_rules.doc_type` CHECK constraint (ปัจจุบันมีแค่ `pr/po_wo/petty_cash/advance/other`)
และเพิ่ม `can_approve_subcontractor` เป็น flag ที่ 10 บนตาราง `customers` — เป็นงาน migration ใหม่ที่ต้อง
ทำตอนเขียนโค้ดจริง (นอกเหนือจากตาราง subcontractor เอง)

---

## สรุปจุดที่ต้องให้ฝ่ายบัญชียืนยันก่อนเขียนโค้ดจริง (เรียงตามความสำคัญ)

- [ ] **WHT ในส่วน 2 คำนวณจากฐานไหน** (มูลค่างวดเต็ม 200,000 หรือหลังหักคืน/retention 160,000) — ผลต่าง
      1,200 บาทต่องวดตัวอย่าง เอกสารนี้ยึดทางที่ 1 (ฐานเต็ม) เป็น default ไว้ก่อน
- [ ] มี WHT บนเงินล่วงหน้า (2.1) หรือไม่ — เอกสารนี้ default เป็น "มี"
- [ ] มี VAT บนเงินล่วงหน้า (2.1) หรือไม่ — ขึ้นกับผู้รับเหมาแต่ละราย เสนอให้ toggle ได้ต่อรายการ
- [ ] ชื่อ/รหัสบัญชี `1160` เหมาะสมหรือไม่ (เลขไม่ชนของเดิม แต่ชื่อ/ตำแหน่งในผังบัญชีต้องยืนยัน)
- [ ] โครงสร้าง `client_subcontract_billings` ที่รวม 3 ประเภทเอกสารไว้ตารางเดียว เห็นด้วยหรือควรแยก
